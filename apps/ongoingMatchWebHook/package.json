{
  "name": "ongoingmatchwebhook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build2": "npx esbuild src/*.ts --bundle --platform=node --outdir=dist --tsconfig=tsconfig.json --external:puppeteer-extra --external:puppeteer-extra-plugin-stealth",
    "build": "npx tsc -b",
    "start": "node dist/index.js",
    "start2": "node dist/seedCompletedMatches.js",
    "dev": "pnpm run build2 && pnpm start",
    "dev2": "pnpm run build2 && pnpm start2"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@repo/common": "workspace:*",
    "@types/axios": "^0.14.4",
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "esbuild": "^0.25.2",
    "is-plain-object": "^5.0.0",
    "kind-of": "^6.0.3",
    "puppeteer": "^24.6.1",
    "puppeteer-core": "^24.6.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "typescript": "^5.8.3"
  }
}
