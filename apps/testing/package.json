{
  "name": "testing",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc -b",
    "build2": "npx esbuild src/*.ts --bundle --platform=node --outdir=dist --tsconfig=tsconfig.json",
    "start": "node dist/index.js",
    "dev": "pnpm run build && pnpm start",
    "dev2": "pnpm run build2 && pnpm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@repo/db": "workspace:*",
    "@types/axios": "^0.14.4",
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "esbuild": "^0.25.2",
    "typescript": "^5.8.3"
  }
}
