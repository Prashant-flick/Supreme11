{
  "name": "webhook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild src/*.ts --bundle --platform=node --outdir=dist --tsconfig=tsconfig.json",
    "build2": "npx tsc -b",
    "start": "node dist/index.js",
    "start2": "node dist/squad.js",
    "dev": "pnpm run build2 && pnpm start",
    "dev2": "pnpm run build2 && pnpm start2"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/axios": "^0.14.4",
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "esbuild": "^0.25.2",
    "puppeteer": "^24.6.1",
    "puppeteer-core": "^24.6.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "typescript": "^5.8.3",
    "@repo/common": "workspace:*"
  }
}
